---
- name: Profile
  connection: local
  gather_facts: false
  hosts: all
  vars:
    sudors:
      vagrant
    ansible_python_interpreter: /usr/bin/python3
  become: true
  become_user: vagrant
  tasks:

   - name: Supress Login Messages
     file:
      path: ~/.hushlogin
      state: touch

     become_user: vagrant
